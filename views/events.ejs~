<% include header %>
<body>
	<% include navbar %>
	<div class="container content">
		<h1 class="white header"> Your Events </h1>
			<a href="/createevent">
				<button type="button" class="btn btn-default btn-lg">
					<span class="glyphicon glyphicon-flag"></span> Create an Event
				</button>
			</a>
			<br>
			<br>

			<% for (var i = 0; i < events.length; i++) { %>
				<div class="panel panel-default nopadding">
				
					<div class="panel-heading row eventrows">
							<div class="col-sm-4">
								<h4><b><img src="../img/sportsmall.png"> </img><%= events[i].name %></b></h4>
							</div>
							<div class="col-sm-5">
								<h4><b>Time:</b> <%= events[i].startTime %></h4>
								<h4><b>Location:</b> <%= events[i].location %></h4>
							</div>
							<div class="col-sm-3">
								<a href="/leaveevent/<%= events[i]._id %>" class="btn btn-danger btn-lg btn-block">Leave Event</a>
								<!-- Use this for the search page
								<div class="btn-group">
								  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
									<h4>Actions <span class="caret"></span></h4>
								  </button>
								  <ul class="dropdown-menu" role="menu">
									<li><a href="#">Leave Event</a></li>
								  </ul>
								</div>
								-->
							</div>
					</div>
					
					<div class="panel-body">
						<h4><b>Event Description:</b></h4>
						<p><%= events[i].description %></p>
						<a href="/event/<%= events[i]._id %>" class="read_more">...click to see more</a><br/>
					</div>

					<div class="panel-footer">
						<h4><b>Who's Attending?</b></h4>
						<% for (var j = 0; j < events[i].users.length; j++) { %>
							<p><%= events[i].users[j].userName %></p>
						<% } %>
					</div>
				</div>
			<% } %>
			
	</div>
	<% include js %>
	<script src="js/events.js"></script>
</body>
</html>
